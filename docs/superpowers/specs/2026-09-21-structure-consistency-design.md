# economic-site 화면·컴포넌트 구조 통일 기획서 (2026-09-21)

> 선행 셋은 전역 규칙이었다 — **무엇을 어디 놓을지**(IA, 9/18) · **어떻게 읽히게 할지**(가독성, 9/19) ·
> **누르면 무슨 일이 일어나는지**(조작·전환, 9/21). 이 문서는 **화면과 컴포넌트 단위의 구조**를 다룬다.
> 사용자 관찰이 출발점이다 — *"페이지별로 컴포넌트가, 화면별로 통일감이 없는 느낌"*. 재보니 느낌이 아니었다.

## 0. 근거

| 측정 | 도구 | 표본 | 산출물 |
|---|---|---|---|
| 위젯·표·차트 인벤토리 | `scripts/audit-widgets.mjs` | 화면 10 × 뷰포트 2 · 위젯 104 · 표 23 · 캔버스 42 | `raw/WIDGET-DIGEST.md` |
| 숫자·단위·자릿수 코드 감사 | 정적 분석(29,812줄) | `index.html` + `js/app0~7.js` | `raw/FORMAT-AUDIT.md` |
| 화면 골격 감사 | `scripts/audit-shell.mjs` | 화면 10 × 뷰포트 2 | `raw/shell-{d,m}.json` |
| 벤치 표 구조 | `scripts/bench-tables.mjs` | 네이버 PC/모바일 · 토스 PC | `raw/bench-tables-{d,m}.json` |

측정 한계: ① 조건부로 숨은 위젯(0px)은 구조를 잴 수 없어 `측정불가`로 남겼다 — 빈 위젯 8건 중 5건이 그 경우다.
② 벤치는 `<table>`을 거의 쓰지 않아(토스 0개) 반복 행 묶음을 표로 간주해 쟀다. ③ 코드 감사의 `toFixed` 303곳 중
12~21곳은 SVG 좌표라 표기 수치가 아니다 — 본문은 보정한 **285곳**을 쓴다.

---

## 1. 진단

### U1 — 숫자 표기를 정하는 메커니즘이 8가지다 (P0)

`raw/FORMAT-AUDIT.md`. 자릿수를 정하는 독립 경로가 여덟이고, 가장 넓은 것이 **하드코딩 `toFixed(n)` 285곳**이다.

| 목적 | 독립 구현 수 | 증상 |
|---|---:|---|
| 지표값 자릿수 고정 | **9** | 로케일·기본자릿수·null 표기가 제각각 |
| 등락률 표기 | **5** | `▲ 1.65%` / `+1.65%` / `(+1.65%)` 세 모양이 공존 |
| 원화 금액 | **3** | `₩1,234,567`(접두) vs `1,234원`(접미) |
| 큰 수 축약 | **4** | 억·조 임계값이 서로 다름 |
| 결측 표기 | **3** | `—` / `-` / 빈 문자열 |

**지표 레지스트리(`window.ECON_IND`, 141행)는 이 목록에 없다.** 호출부 11곳은 전부 내비게이션·라벨·검색용이고
표기에는 한 번도 쓰이지 않는다. 레지스트리에 `unit`은 50%만 있고 `decimals`/`scale`/`sign` 필드는 아예 없다.

실제로 어긋나는 자리(전부 파일:줄 확인):

- **C1 USD/KRW 네 가지 표기** — `1,372.58`(`app1:12638`) · `1,372.6`(`app1:9250`) · `1372.6`(천단위 없음, `app6:294,521`) · `1,372.58원`(`app3:68`)
- **C3 실동작 오류** — `app1:12624`의 지역 `fmt(v, kind)`에 **숫자를 넘기는 호출이 11곳**이다. `INDICATOR_DECIMALS[4]`가 `undefined`라 전부 2자리로 폴백한다. 그래서 JPY(100)/KRW가 `8.7292` → **`8.73`**, EUR/USD가 `1.1477` → **`1.15`**로 나온다. `data.json`은 4자리를 갖고 있는데 화면이 버리고 있다.
- **C6 "조원" 자릿수 4종** — 같은 국민연금 AUM이 1자리(`app1:10080`)·로케일 기본(`app1:9550`)·원시값(`app1:10099`)·0자리(`app1:13282`)
- **C7 VKOSPI 무포맷** — `app6:296`이 값을 그대로 문자열화해 데이터에 따라 자릿수가 흔들린다

### U2 — 위젯 헤더 구성이 10종이다 (P0)

`raw/shell-d.json`. 위젯 104개(1440)의 헤더 조각 조합:

| 조합(제목/별/출처/새로고침/접기) | 개수 |
|---|---:|
| 제목 · — · — · 새로고침 · 접기 | 64 |
| 제목 없음(조각 0) | **10** |
| 제목 · — · — · 새로고침 | 7 |
| 제목 · 별 · 출처 · 새로고침 · 접기 | 6 |
| 그 밖 6종 | 17 |

기능별로 보면 제목 90% · `data-asof` 90% · 새로고침 73% · **즐겨찾기 별 9%**다.
별은 사실상 특권 위젯 9개에만 있다 — 사용자는 어떤 위젯이 관심 목록에 담기는지 예측할 수 없다.

**정정(구현 중 확인)**: 처음에 "제목 굵기가 600 ×80 / 700 ×14로 갈린다"고 적었으나, 뜯어보니 두 역할이었다 —
600은 **KPI 카드 라벨**(`span.widget-title.econ-stat__label`), 700은 **위젯 제목**(`h3.widget-title`).
굵기는 각 역할에 맞는 값이고, 진짜 문제는 **`.widget-title` 한 클래스가 두 역할을 겸한다**는 것이다.
그래서 마킹 코드(`econMarkFavorites`·`econMarkSources`)가 매번 `closest('button, .kpi-card …')`로 KPI를 걸러낸다.
같은 이유로 여백 4종 중 `14px`(11개)는 CLAUDE.md에 기록된 **의도된 예외**(`.pad-14`)다 — 통일 대상이 아니다.
남는 진짜 변형은 `0px ×2`와 복합 여백 3개뿐이다. 반경 12px·데스크톱 제목 크기 14px는 이미 통일돼 있다.
모바일 제목 크기 **14px ×73 / 16px ×12**는 같은 겹쳐쓰기의 그림자이므로 클래스 분리와 함께 본다.

### U3 — 위젯 높이가 49종, 내용 종류 중 산문이 최다 (P1)

표시 중인 위젯 70개의 높이가 **서로 다른 값 49종**(36px ~ 1,353px, 중앙 334px).
내용 종류는 **산문 34 · 차트 27 · 표 23 · 혼합 8 · 빈 8 · KPI 3 · 목록 1**이다.
데이터 대시보드에서 산문이 최다라는 사실 자체가 구조 문제다 — 설명이 값과 같은 층에 섞여 있다.

### U4 — 표 규격이 표마다 다르다 (P1)

표 23개(1440) 실측:

- 열 수 3~5, 본문 정렬은 `start|right|right` 계열이지만 **첫 두 열이 start 인지 한 열만 start 인지가 표마다 다르다**
- **단위가 셀에 섞인 비율이 0.2~0.75**로 제각각(같은 성격의 Top10 표끼리도 0.2 vs 0.42 vs 0.5)
- `aria-sort` 없는 표가 남아 있다(`equity` 투자자별 순매매 등)
- 벤치 대비: 네이버 PC 행 높이 **34~40px**, 토스 **36~40px**, 네이버 모바일 **42~58px**. 우리는 행 높이 규격이 없다

벤치의 규칙은 단순하다 — **단위는 열 머리에, 값 칸에는 숫자만**. 네이버 시장지표가 소수 4자리를 쓰는 곳은
환율 한 종류뿐이고 나머지는 2자리로 고정돼 있다(`raw/bench-tables-d.json`).

### U5 — 천단위 구분기호가 화면마다 빠진다 (P1)

천단위 후보(정수부 4자리 이상, 연도·날짜 제외) 대비 콤마가 붙은 비율:

| 화면 | 후보 | 콤마 | 비율 |
|---|---:|---:|---:|
| dashboard · equity · market · macro · investor | 32·64·9·3·4 | 전부 | **1.00** |
| merlens | 13 | 7 | **0.54** |
| realestate | 12 | 2 | **0.17** |

**정정(구현 중 확인)**: 이 비율은 과장돼 있었다. 콤마 없는 값을 사람이 읽을 수 있게 뽑아 보니
realestate 14건 중 **12건이 `202608`·`202607` 같은 기간 코드와 출처 표기**였다 — 수치가 아니라 식별자다.
진짜 누락은 두 종류였다: ① 메르 렌즈의 지표 값(`7704.82` · `8200` · `1,372.93원`) ② FX 52주 범위 칸.
그래서 게이트(S13)는 문서 전체가 아니라 **값이 사는 자리**(`td`·`.econ-num`·`.econ-numcell`·KPI 값)에서만 센다.
문서 전체를 훑는 첫 판은 식별자를 결함으로 세어 0%를 냈다.

### U4-b — 표 문법이 표마다 다르다 (P1, 감사 보강)

`raw/WIDGET-DIGEST.md` 전수:

- **정렬 CSS 키워드 4종 혼용** — `start` 36열 · `right` 45열 · `left` 8열 · `center` 30열.
  `start`와 `left`는 결과가 같은데 표마다 다른 단어를 쓴다.
- **빈칸 표기** — `—` 40칸 vs **빈 문자열 264칸**(`-`·`N/A`는 0). 값이 없는 것과 아직 안 온 것을 구별할 수 없다.
- 행이 있는 표 24개 중 **6개는 `aria-sort` 없음**, **4곳은 `thead` 없이 `<table>`을 레이아웃으로 쓴다**.

### U9 — 같은 역할이 컴포넌트 2~5종으로 갈린다 (P1)

- `.tab-btn` 65개 중 **45개는 `seed-chip__root`, 15개는 `seed-tabs__trigger`** — 같은 '탭'인데 두 부품.
- 버튼: `btn-plain` 430 · `btn-inline` 389 · `seed-action-button` 83 · `w-toggle-btn` 61 · `mer-matrix-btn` 35.
- 위젯 제목 마크업 3종(`h3` · `span.econ-stat__label` · `div.seed-callout__title`).
- 레지스트리 API 11개 중 **7개는 호출부가 없다**(`byPath`·`tier`·`asset`·`topic`·`label`·`canonicalOf`·`newsKeyOf`).

### U10 — 화면이 뷰포트·진입 순서에 따라 다른 것을 보여준다 (P2)

- **macro는 데스크톱 위젯 15개 → 모바일 6개**. 10분류 블록이 모바일에서 탭 하나로 변형된다(지연 렌더 아님).
- **market은 첫 화면에서 위젯 16/20이 높이 0** — 탭 4개 중 하나만 열려 있고 차트 9개 중 8개는 탭을 눌러야 빌드된다.
- **차트 7개가 어느 화면을 열든 항상 생성된다**(`compareChart`·`spark1~3`·`spark-rate`·`rateHistoryChart`·`fearChart`) —
  캔버스가 0개인 `notes`·`study`에서도 살아 있다.

### U6 — 차트 규격이 없다 (P2)

캔버스 42개 중 Chart.js 인스턴스가 붙은 것만 놓고 봐도:

- **범례 표시가 제각각** — dashboard `mainChart` Y, macro 4종 전부 N, merlens 8종 전부 N, investor 혼재
- **y축 0 시작**이 대부분 N, 일부만 Y(`investorChart`, `npsReturnChart`)
- **라벨 수 9~274** — 같은 라인 차트인데 x축 밀도가 30배 차이
- 스파크라인 4종은 230×40으로 통일돼 있다(이건 잘 된 축)

### U7 — 빈 위젯 8개 · 수집만 하고 화면 없는 지표 16건 (P2)

빈 위젯 8건 중 5건은 조건부 숨김(정상)이고, **`notes`의 「전체 요약 / 종합 의견」은 표시 중인데 본문이 빈 문자열**이다.
레지스트리에는 `onScreen:false`/`collectOnly` 인 행이 **16건**(BTC + 거시 14 + 부동산 1) 남아 있다 —
9/18 기획이 "남은 것"으로 넘긴 목록 그대로다.

### U8 — 화면 머리가 0~4줄이다 (P2)

본문 첫 위젯 앞의 컨트롤 줄 수: `equity`·`notes`·`study` **0줄** / `dashboard`·`market`·`calendar` 1줄 /
`macro`·`investor` 2줄 / `realestate` 3줄 / `merlens` **4줄**. 화면마다 시작 모양이 다르다.

---

## 2. 원칙

1. **표기는 지표가 정한다.** 자릿수·단위·부호는 화면이 아니라 레지스트리 행에 적혀 있고, 화면은 그것을 읽어 쓴다.
2. **위젯은 한 벌이다.** 헤더 조각의 순서와 유무가 위젯마다 달라지지 않는다.
3. **값 칸에는 값만.** 단위는 열 머리에, 설명은 값 옆이 아니라 아래 한 줄에.
4. **숨길 거면 지우고, 남길 거면 보여준다.** 조건부 0px 위젯과 `onScreen:false` 지표는 둘 중 하나로 결정한다.

---

## 3. 개편안

### S1 — 표기 레지스트리와 단일 포매터 (U1) · P0

**레지스트리에 표기 필드를 넣는다.** `scripts/build_indicators.py` 가 생성하는 행에 추가:

| 필드 | 뜻 | 예 |
|---|---|---|
| `unit` | 화면에 쓸 단위(기존, 커버리지 50% → 100%) | `%` · `원` · `달러` · `pt` · `조원` |
| `decimals` | 소수 자릿수(정수 고정) | `kospi` 2 · `usdkrw` 2 · `jpykrw` **4** · `unemployment` 1 |
| `scale` | 값에 곱할 배율과 그때의 단위 | `{by: 1e-12, unit: '조원'}` |
| `sign` | 등락 표기 방식 | `arrow`(▲▼) · `signed`(+/-) · `none` |

**포매터 진입점을 하나로.** `econFmt(id, value, opts)` 를 `js/app0.js` 옆(`js/app1.js` 상단)에 두고,
기존 9개 지표값 포매터는 전부 이 함수에 위임한다. 값 자체를 바꾸지 않는 얇은 층이라 회귀 위험이 낮다.

```js
econFmt('usdkrw', 1372.58)        // '1,372.58'
econFmt('usdkrw', 1372.58, {unit: true})   // '1,372.58원'
econFmt('jpykrw', 8.7292)         // '8.7292'   ← 지금은 '8.73'
econFmt.chg('kospi', 1.65)        // '▲ 1.65%'
```

**C3(실동작 오류)는 P0 안에서 바로 고친다** — `fmt(v, 4)` 처럼 숫자를 kind 자리에 넘기는 11곳이 대상이다.
이건 통일 이전에 **데이터를 버리고 있는 버그**다.

**1440/390 차이** — 없다. 표기는 뷰포트와 무관하다.

### S2 — 위젯 한 벌 (U2) · P0

헤더 조각의 **순서를 고정**한다: `제목 → 출처(다른 화면이 원본일 때) → 신선도 → 별 → 새로고침 → 접기`.
유무 규칙:

| 조각 | 규칙 |
|---|---|
| 제목 | **항상**. 지금 제목 없는 위젯 10개는 제목을 주거나 위젯이 아닌 블록으로 내린다 |
| `data-asof` | 데이터 위젯이면 항상(현행 90% → 100%, G8 이 이미 잰다) |
| 별 | **레지스트리가 아는 지표 제목이면 항상**(현행 9% → 대상 전부). 특권 위젯 9개만 담기는 현 상태를 끝낸다 |
| 새로고침 | 스스로 다시 부를 수 있는 위젯만. 정적 위젯에는 두지 않는다 |
| 접기 | 높이 240px 이상인 위젯만 |

**굵기는 건드리지 않는다**(위 정정 참조 — 600/700은 역할이 다르다). 대신 **`.widget-title` 의 두 역할을 분리**한다:
위젯 제목은 `h3.widget-title`, KPI 라벨은 `.econ-stat__label` 만 남기고 `widget-title` 을 뗀다.
그래야 마킹 코드가 매번 `closest()` 로 KPI 를 걸러내지 않고, 구조 감사도 둘을 섞어 세지 않는다.
여백은 `16px`(모바일 `12px`)이 기본이고 `.pad-14` 는 문서화된 예외로 유지, `0px ×2`·복합 3개만 정리한다.

### S3 — 표 규격 (U4·U5) · P1

- **정렬**: 첫 열(이름) `start`, 나머지 숫자 열 `right`. 이름이 두 칸(코드·이름)이면 두 칸 모두 `start`.
- **단위는 열 머리에** — `현재가(원)` · `등락률(%)` · `거래대금(억)`. 값 칸에는 숫자만(현행 단위셀 비율 0.2~0.75 → 0).
- **빈칸은 `—` 하나**(현행 `-`·`N/A`·빈 문자열 혼재).
- **행 높이**: 데스크톱 36px · 모바일 44px(벤치 34~40 / 42~58).
- **모든 표에 `aria-sort`** — 정렬 표식은 9/21 개편에서 이미 붙었다(G14).
- 천단위 구분기호는 S1 의 `econFmt` 가 전담한다 — realestate 0.17 · merlens 0.54 가 여기서 해소된다.

### S4 — 차트 규격 (U6) · P2

- **범례**: 데이터셋 2개 이상이면 표시, 1개면 숨김(현행 macro 4종은 1개인데 숨김 = 맞고, merlens 8종은 3~6개인데 숨김 = 틀림).
- **y축 0 시작**: 막대는 항상 0, 선은 값 범위(현행 규칙 없음).
- **x축 라벨 상한**: 좁은 화면 6, 넓은 화면 12(현행 9~274).
- 스파크라인 230×40 규격은 유지한다.

### S5 — 빈 위젯·미노출 지표 (U7) · P2

- `notes` 「전체 요약 / 종합 의견」처럼 **표시 중인데 빈** 위젯은 빈 상태 문구를 넣는다.
- 조건부 0px 위젯 5건은 **접힘**으로 바꾼다 — 있는데 안 보이는 것과 없는 것은 다르다.
- `onScreen:false` 16건은 행마다 **노출 / 폐기**를 적는다. 거시 14건은 대부분 `macro` 상세 표에 한 줄씩 들어갈 수 있다.

### S6 — 화면 머리 규격 (U8) · P2

`제목 → 2차 탭 → 필터 칩` 순서로, **최대 2줄**. `merlens` 4줄은 목차와 설명을 접어 2줄로,
`equity`·`notes`·`study` 0줄은 화면 제목(H1)을 세워 1줄로 만든다. H2 복원(9/21 C11)과 함께 간다.

---

## 4. 로드맵

| 단계 | 내용 | 근거 |
|---|---|---|
| **P0** | S1 표기 레지스트리 + `econFmt` + C3 버그 · S2 위젯 한 벌 | U1·U2 |
| **P1** | S3 표 규격 | U4·U5 |
| **P2** | S4 차트 · S5 빈 위젯·미노출 지표 · S6 화면 머리 | U6·U7·U8 |

P0 가 먼저인 이유: 표기가 흔들리면 표 규격(S3)을 맞춰도 같은 값이 화면마다 달라 보인다.
그리고 C3 은 통일 문제가 아니라 **데이터를 버리는 버그**다.

---

## 5. 게이트

새 파일 `tests/ui/structure.mjs`.

| 기호 | 기준 | 현재 | 목표 |
|---|---|---|---|
| **S10** | 같은 지표의 표기가 화면 간 동일 | USD/KRW 4종 | **1종** |
| **S11** | 위젯 헤더 조합 종류 | 10종 | **≤3종**(데이터/차트/산문) |
| **S12** | 제목 굵기·여백 종류 | 굵기 2 · 여백 4 | **각 1**(모바일 여백 별도 1) |
| **S13** | 천단위 구분기호 비율 | realestate 0.17 · merlens 0.54 | **전 화면 1.00** |
| **S14** | 표의 단위셀 비율 | 0.2~0.75 | **0**(단위는 열 머리) |
| **S15** | 레지스트리 `unit`/`decimals` 커버리지 | unit 50% · decimals 없음 | **각 100%** |

---

## 6. 비채택

| 항목 | 이유 |
|---|---|
| 위젯 높이 49종 → 규격화 | 내용 길이가 정하는 값이다. 높이를 맞추면 빈 공간이 생긴다. 접힘 임계(240px)만 규정한다 |
| 산문 위젯 34개 제거 | 메르 렌즈·분석 노트는 산문이 본문이다. 값과 섞인 자리만 S3 로 정리한다 |
| 표를 `<table>` 로 통일(벤치는 div) | 우리 표는 이미 `<table>` 이고 정렬·스크린리더가 거기 붙어 있다. 벤치를 따라갈 이유가 없다 |
| `toFixed` 285곳 전면 치환 | P0 는 **지표값 경로**만 `econFmt` 로 모은다. 차트 축·툴팁·SVG 좌표까지 건드리면 회귀 위험이 이익을 넘는다 |

---

## 7. 구현 기록 — P0 (2026-09-21)

| 한 일 | 전 | 후 |
|---|---|---|
| 레지스트리 표기 필드 | `unit` 50% · `decimals` **없음** | **지표 122행 전부 `decimals`**, `unit` 73행(생성기 `FORMAT_BY_ASSET`/`FORMAT_BY_ID`) |
| 포매터 진입점 | 지표값 포매터 9종 | `econFmt`/`econFmtChg` 신설, `fmtIndicator` 가 위임 |
| JPY·EUR 자릿수(C3) | `8.73` · `1.15`(데이터를 버림) | **`8.7226` · `1.1477`** |
| FX 표 ↔ 상태 | 표가 이전 스냅샷(상태 8.7226 / 표 870.00) | **5/5 일치** |
| FX 52주 범위 | 하드코딩 상수(현재가 872인데 저가 920) | **history 실측**(969.32~854.70), 없으면 `—` |
| FX 등락 열 | 퍼센트에 ×100(`-120.00`) | **`-1.20%`** |
| 메르 렌즈 값 표기 | `7704.82` · `8200` · `1372.93원` | **천단위 적용** |
| 게이트 | 없음 | `tests/ui/structure.mjs`(S10·S13·S15·S16) |

**구현하며 드러난 것 넷** — 진단이 틀린 것도 그대로 적는다.

1. **제목 굵기 600/700은 결함이 아니었다.** 600은 KPI 카드 라벨, 700은 위젯 제목이다. 진짜 문제는
   `.widget-title` 한 클래스가 두 역할을 겸하는 것이고, 그래서 마킹 코드가 매번 `closest()` 로 KPI 를 걸러낸다(§U2 정정).
2. **천단위 누락 비율도 과장돼 있었다.** realestate 14건 중 12건이 기간 코드였다(§U5 정정).
   진짜 누락은 메르 렌즈 값과 FX 52주 칸 두 곳이었고 둘 다 고쳤다.
3. **더 큰 것이 옆에서 나왔다** — `data.json` 적용 경로가 FX 표를 다시 그리지 않아, 카드와 표가
   **서로 다른 시점의 환율**을 보여주고 있었다. 표기 통일을 재다가 값 불일치를 찾은 셈이다.
4. **52주 범위는 전부 가짜였다.** `fxPairs`·`comData` 정의부의 상수가 그대로 화면에 남아 현재가가
   52주 저가보다 낮은 모순이 보였다. FX 는 history 로 실측하게 고쳤고, **원자재(`comData`)는 같은 문제가 남아 있다** — P1.

### 2차 (같은 날) — 감사 에이전트 보고 반영

- **메르 렌즈 % 자릿수 4종 → 2자리 고정**(`4%`·`4.1%`·`5.29%`·`0.977%` → 전부 `4.00%` 꼴). 게이트 **S17** 신설.
- **market FX 차트 빈 캔버스 보고는 재현되지 않았다** — 세 진입 경로(단독 / dashboard→market / dashboard→equity→market)
  모두 인스턴스가 있고 픽셀 1,755개가 그려진다. 감사 시점이 이 문서의 P0(표 재렌더 연결) **이전**이라,
  같은 수정으로 해소된 것으로 본다. 재발하면 `charts.fxChart` 유무와 캔버스 높이(220 정상 / 389 이상)로 구별한다.
- 감사가 새로 준 수치를 §U4-b·U9·U10 으로 진단에 넣었다(정렬 키워드 4종 · 빈칸 264칸 · 탭 부품 2종 · macro 15→6 등).

### 3차 — 남은 항목 (P1~P2)

| 항목 | 한 일 | 확인 |
|---|---|---|
| **원자재·FX 상세 52주** | FX 표에 이어 **원자재 카드와 FX 상세 블록**도 `comData`/`fxPairs` 상수를 읽고 있었다. `comHistoryKey()` 로 history 경로를 얻어 실측한다 | WTI 52주 `$55.27~$112.95`(현재 $92.34) |
| **막대 y축 0 시작** | `chartOpts(color, isBar, yMin)` 의 **`isBar` 인자가 쓰이지 않아** `beginAtZero: false` 가 고정이었다 — gdpMacro 가 0 아닌 축에 그려졌다 | 막대 차트 y축 0 시작 |
| **미노출 지표 15건 → 0** | `macroIndicators` 에 14행(미국 GDP·장단기 금리차·신규 실업수당·소비자심리·RRP·TGA·지준금·브로드 달러·일본 10Y·유로존/중국/독일/영국 GDP·한국 외환보유액) + 부동산 거래량 1행 추가. 생성기가 `onScreen` 을 자동 갱신한다 | **미노출 0건**(S18) |
| **`rankingsKr` 죽은 경로 오탐** | 토스 수집기(로컬 PC)가 켜져 있을 때만 실리는 키다. 생성기에 `optional` 플래그를 두고 검사에서 뺐다 — 없는 것이 정상인 자리를 결함으로 세면 게이트가 늘 빨갛다 | `--check` 깨끗 |
| **거시 모바일 밀도** | 카드가 47→62 로 늘어 390 거시가 4.05화면(M5 상한 4.0). 분류 안 카드를 **6개 + 「나머지 N개 보기」** 로 바꿨다(접으면 기본 분류를 아예 못 본다) | **4.05 → 3.41화면** |
| **메르 렌즈 자릿수** | % 2자리 고정에 더해, 소수가 있는 값만 자릿수를 고정한다. 정수에 `.00` 을 붙이면 트리거 레벨 텍스트와 또 어긋난다 | G6 자릿수 불일치 0 |

**비채택 2건(측정으로 기각)**

- **빈칸 `—` 통일** — 빈 칸 264개의 정체는 **메르 렌즈 민감도 매트릭스 280칸**이었고, 그 위젯 제목이
  「빈 칸=언급 없음」이라고 이미 말한다. 의도된 빈칸이다. 다른 화면은 화면당 1칸뿐 — 채우면 매트릭스가
  기호로 가득 차 오히려 읽기 나빠진다.
- **정렬 키워드 통일(`left`→`start`)** — `start` 와 `left` 는 LTR 에서 결과가 같다. 인라인 스타일을
  일괄 치환하는 위험 대비 시각적 이익이 0 이다. `center` 30열은 배지·상태 칸이라 의도된 값이다.

**남은 것**: S3 표 규격(단위를 열 머리로 · 행 높이 · `thead` 없는 표 4곳) · S4 차트 범례·x축 라벨 상한 ·
S6 화면 머리 · U9 컴포넌트 통합(탭 부품 2종·버튼 5종) · U10 항상 생성되는 차트 7개.