# 메르 리스크 렌즈 — 추출 품질 사람 표본 검수 (P5-3)

`scripts/tests/test_mer_extract_quality.py` 가 기계로 검증 가능한 항목(인용 길이, enum 범위,
logNo 중복, 날짜 형식, risk_flags 허용 목록, econ 비율)은 이미 자동 검사한다. 이 문서는 **그
테스트가 검사할 수 없는 항목**만 다룬다.

## 커버리지 (2026-09-15 기준)

- 코퍼스 전체 810편 중 **376편**이 `mer_extract_cache.jsonl` 로 구조화 완료.
- 나머지 **434편**은 LLM 키(`ANTHROPIC_API_KEY` → `GEMINI_API_KEY`) 대기 — `scripts/mer_extract.py --limit N`
  으로 회차를 나눠 백필한다(`scripts/mer_extract.py` 상단 주석 참고).
- `econ=true` 326/376 (87%) — 자동 테스트가 70~95% 대역으로 고정한 수치와 일치.

## 사람이 30편 표본으로 확인해야 하는 항목

원문 자체는 이 저장소에 저장하지 않는다(공개 저장소 — `scripts/fetch_merblog.py` 주석 참고).
그래서 **인용이 원문에 실재하는지**는 코드로 재검증할 수 없다 — `scripts/mer_extract.py` 의
`quote_is_real()` 이 추출 시점에 한 번 원문과 대조해 걸러내지만, 그 이후 원문이 사라지므로
캐시 파일만 놓고는 "이 인용이 정말 그 글에 있었나"를 다시 확인할 방법이 없다.

사람이 직접 `https://blog.naver.com/ranto28/{logNo}` 원문을 열어 30편 표본(예: 날짜순 12편
간격 샘플링)에서 아래를 확인할 것:

1. **인용 실재성** — `indicators[].quote` / `thresholds[].quote` / `impacts[].quote` 가 해당 글에
   실제로 있는 문장인지(요약·창작이 아닌지).
2. **chain 의 개연성** — `chain` 필드(전이경로 한 줄)가 인용된 근거들로부터 합리적으로 도출되는지,
   아니면 근거와 무관한 문장이 붙었는지.
3. **stance.why 의 정합성** — `view` 값(−2~+2)과 `why` 서술이 방향상 일치하는지(예: view=+2 인데
   why 가 부정적 내용).
4. **one_liner 의 대표성** — 글 전체 논지를 한 줄이 왜곡 없이 요약하는지(추출 로직은
   `one_liner_last()` 로 "마지막 매치"만 신뢰하는데, 그 마지막 매치가 실제로 그 글의 결론인지는
   내용을 읽어야 판단 가능).

## 자동 검사에서 발견된 것 (참고 — 표본 검수 우선순위에 반영)

`test_mer_extract_quality.py` 를 376행 전체에 실측 실행한 결과(2026-09-15, 이 저장소가 다른
세션과 동시에 작업 중이던 시점 — `mer_extract_cache.jsonl` 은 공유 파일이라 재확인 시 값이
바뀔 수 있음에 유의):

- 이번 실측에서는 80자 인용, stance.view 범위, impacts.direction(`+`/`-`/`±`), strength(1~3),
  horizon(3종), risk_flags(10종 허용 목록), logNo 중복, 날짜 형식, econ 비율(87%) 전부
  376건 위반 0건.
- **직전 실측(같은 날)에서는 `impacts[].direction` 위반이 1건 있었다** — logNo `224339898546`
  (2026-07-08, `_meta.source: "research-backfill-376"`)의 한 impact 가 `direction: "0"` 을
  가졌다(허용값은 `+`/`-`/`±` 뿐, `scripts/mer_extract.py` 의 `_VALID_DIR` 가 이를 강제하지만
  이 레코드는 그 검증 경로를 거치지 않고 별도로 채워진 것으로 보인다). 두 번째 실측에서는 이
  레코드가 사라져 있었다 — 이 파일에 동시에 쓰고 있는 다른 파이프라인(백필/재집계)이 있다는
  뜻이다. 표본 검수 시 `224339898546` 을 먼저 열어 지금도 정상인지 확인할 것 — enum 위반이
  있었던 레코드는 다른 필드 품질도 함께 의심할 이유가 된다. (2026-09-15 표본 검수에서
  재확인: 이 레코드는 지금도 존재하고 `direction` 위반은 없다 — 아래 참고.)

## 2026-09-15 표본 검수 결과 (기계적 대조 실측)

원문을 실제로 네이버에서 다시 받아 인용·한줄코멘트·트리거 숫자를 캐시와 대조했다(위
"사람이 확인해야 하는 항목"의 1번을 기계로 대신한 것 — 2·3번 해석적 판단은 여전히
사람 몫으로 남는다, 아래 "여전히 사람 몫" 참고).

### 표본 구성

- `mer_extract_cache.jsonl` 736편(`claude-opus-5` 376 / `gemini-3.1-flash-lite` 360)에서
  `_meta.model` 별 **15편씩, 총 30편**을 층화추출.
- 방법: 각 모델 안에서 글을 `date[:7]`(연-월)로 묶고, 월별 리스트를 `random.Random(20260915)`
  로 셔플한 뒤, 월을 정렬 순서로 라운드로빈 순회하며 한 편씩 뽑아 15편이 채워질 때까지
  반복(월별 건수에 비례 배분하지 않고 "달마다 고르게" 뽑히도록 함). 시드 고정으로 재현 가능.
- 두 모델은 커버 기간 자체가 겹치지 않는다 — `gemini-3.1-flash-lite`는 2025-09,
  2025-11~2026-02, 2026-04~05, 2026-09(2편)에 걸쳐 있고, `claude-opus-5`는 2025-10,
  2026-03, 2026-06~09에 걸쳐 있다(같은 달에 두 모델이 섞여 있는 경우가 거의 없음 — 백필이
  기간을 나눠 진행됐다는 뜻).
- 네트워크 실패 **0건** — 30편 전부 원문 수신 성공, 표본에서 뺀 글 없음.
- 뽑힌 30편: gemini 15편 — logNo 224016372320·224084511045·224120654921·224164379641·
  224180791181·224232148996·224296458120·224412030235·224014206322·224070017995·
  224108166352·224141331015·224177599167·224265077682·224295249964. opus 15편 — logNo
  224049461132·224207570942·224304099311·224353113944·224371367594·224402375244·
  224032232941·224224261905·224321533799·224333132194·224369325535·224400415287·
  224053950079·224233944363·224321684736.

### 방법(재현 가능하도록)

- 원문 재수신: `scripts/merblog_lib.py::extract_fulltext()` + `scripts/mer_extract.py::fetch_body()`
  를 **그대로 import**해서 사용(새로 파싱 로직을 짜지 않음). 요청 간 0.4초 sleep.
- 인용 실재성: `indicators[].quote` / `thresholds[].quote` / `impacts[].quote` 를
  `scripts/mer_extract.py::quote_is_real()` 과 동일한 방식(`re.sub(r"\s+","",...)` 로 공백 제거
  후 부분문자열 검사, 80자 절단 `…` 는 그 앞부분만 비교)으로 원문과 대조.
- 한줄 코멘트: 원문에 `scripts/mer_extract.py::one_liner_last()` 를 다시 돌려 캐시의
  `one_liner` 와 공백 무시 비교.
- 트리거 숫자: `thresholds[].level` 에서 숫자(`[\d,.]+`)를 뽑아 원문에 그 숫자 문자열이
  있는지 확인.
- 검수 스크립트는 세션 스크래치패드에만 존재하고(`review_sample.py`, `diagnose_fails.py`)
  이 저장소에는 커밋하지 않았다. `mer_extract_cache.jsonl`·`scripts/*`·`data.json` 은 전혀
  수정하지 않았다(읽기만 함), `validate_data.py` 도 실행하지 않았다.

### 결과 — 모델별 (실측치만)

| 항목 | gemini-3.1-flash-lite (n=15) | claude-opus-5 (n=15) |
|---|---|---|
| 검사된 quote 수 | 46 | 110 |
| **인용 실재율** | 46/46 = **100.0%** | 101/110 = **91.8%** |
| one_liner 일치율 | 15/15 = **100.0%** | 11/15 = **73.3%** |
| 트리거 숫자(level) 일치율 | 2/2 = 100.0% | 13/13 = 100.0% |

### 판정

**두 묶음 모두 인용 실재율 ≥90% → 합격**(기획안 §5.4 기준 그대로 적용).

다만 `claude-opus-5`(정독본) 쪽 인용 실패 8건은 **원인이 하나로 수렴한다**: 캐시 전체를
`_meta` 로 나눠 보면(736편 전수 확인),

| `_meta.model` | `extractedAt` 있음 | `source` 있음 |
|---|---|---|
| gemini-3.1-flash-lite (360) | 360/360 | 0/360 |
| claude-opus-5 (376) | 0/376 | 376/376(`"research-backfill-376"` 등) |

즉 **`claude-opus-5` 376편은 `scripts/mer_extract.py` 의 `sanitize()`/`quote_is_real()` 게이트를
거치지 않고 채워졌다** — 기획 단계에 별도 경로(정독 백필)로 캐시에 직접 들어간 레코드들이라,
추출 시점에 원문과 자동 대조된 적이 없다. 반대로 `gemini-3.1-flash-lite` 360편은 전부
표준 파이프라인을 거쳤으므로 인용 실재율 100%는 "그 모델이 더 정확해서"가 아니라 **게이트를
통과한 것만 남았기 때문**이다(사실 이 게이트가 없었다면 gemini 쪽도 이 정도로 만점은 아니었을
것 — 검증되지 않은 표본이므로 단정은 어렵다). 이 구분은 "모델 비교"라기보다 **"게이트를 거친
배치 vs 거치지 않은 배치" 비교**로 읽는 것이 정확하다.

### 실패 사례 — claude-opus-5, 인용 8건 (로그No · 필드 · 캐시 인용 · 원문 대조)

전부 **날조(원문에 없는 사실 창작)는 아니었다** — 원문에 실제 있는 두 구간을 이어 붙이며
중간의 부연(괄호 환산, 시각 표기, "또는 ~" 대안구, 절 하나)을 통째로 빼서 "연속된
문자열"이라는 규칙을 어긴 경우가 6건, 어미만 바꿔 쓴 경우(패러프레이즈)가 1건, 문자 하나
누락이 1건. 나열:

1. **224207570942**(2026-03-07, 중국배가 호르무즈해협을 통과하는 중) `indicators`
   — 캐시: `"KSL HENGYANG라는 배인데, 급변침을 하면서 11노트 속도로 호르무즈해협을
   빠져나가고 있음"` / 원문: `"KSL HENGYANG라는 배인데, **2026년 3월 7일 오전 8시경,**
   급변침을 하면서 11노트 속도로 호르무즈해협을 빠져나가고 있음"` — 시각 표기 구간이
   빠진 채 앞뒤가 이어 붙었다.
2. **224402375244**(2026-09-07, 2026년 한국형 ISA 역주행 상황 A/S) `indicators` — 캐시:
   `"전체 가입자 973만명의 기존 ISA 혜택을 같이 깎은 것**이다**"` / 원문: `"...같이 깎은
   것**임**."` — 어미 패러프레이즈(사실 왜곡은 없음).
3. **224224261905**(2026-03-21, 호르무즈해협이 막혀도 200일 이상을 버틸수 있나 A/S)
   3개 필드 모두 실패 — `indicators`: 캐시 `"이런 VLCC가 한 달에 40~50척씩..."` / 원문
   `"이런 VLCC(**매우큰유조선**)가 한 달에..."`(괄호 설명 누락). `thresholds`: 캐시
   `"...천연가스 9일분이라, 그렇게 여유 있는..."` / 원문 `"...9일분이라, **추가비축분이
   좀더 있다고 하더라도** 그렇게 여유 있는..."`(절 누락). `impacts`: 캐시 `"...플랜B**를**
   만들어야 한다"` / 원문 `"...플랜B **또는 비상플랜을** 만들어야 한다"고 강조했다.`(대안구
   누락 + 재구성).
4. **224333132194**(2026-07-02, 국민연금 리밸런싱) 2개 필드 실패 — `indicators`: 캐시
   `"...4조5000억원으로 줄고, 하루 매도규모는..."` / 원문 `"...줄고, **20영업일에 나눠서
   팔게 되면서** 하루 매도규모는..."`(절 누락). `thresholds`: 캐시 `'당월 10영업일...'`
   (홑따옴표 `'`·`'`) / 원문은 둥근따옴표 `'당월 10영업일...'`(U+2018/2019) — **이 건은
   내용이 완전히 일치하고 따옴표 글리프만 다르다**(공백만 제거하는 대조 방식의 한계로
   생긴 오탐에 가깝다 — 이 1건을 실재로 다시 세면 opus 인용 실재율은 102/110=92.7%).
5. **224400415287**(2026-09-04, 경영권 전쟁 본게임) `indicators` — 캐시
   `"...2024~2038**까지**..."` / 원문 `"...2024~2038**년까지**..."` — 글자 하나("년")
   누락.
6. **224233944363**(2026-03-30, 스타벅스·빽다방 커피값) `indicators` — 캐시
   `"...540만원인데, 두리안은 2700~3800만원을 벌 수 있어"` / 원문 `"...540만원(**1억동**)
   인데, 두리안은 2700~3800만원(**5~7억동**)을 벌 수 있어"` — 원화 환산 괄호 2개 누락.

**해석**: 이 배치가 게이트를 거치지 않았다는 사실(위 표)과 정확히 들어맞는다 —
`quote_is_real()` 이 살아 있었다면 이런 "부연 생략형" 인용도 (엄격히는) `q in norm(body)`
가 거짓이라 **버려졌을 항목들**이다. 즉 원인은 모델 품질이 아니라 이 376편이 그 필터를
안 거친 것.

### one_liner 불일치 4건(claude-opus-5) — 원인 특정 불가 + 별도 버그 1건 발견

`one_liner_match=False` 4건 전부 **캐시 값이 재계산값의 부분집합**이었다(캐시에 없는
문장이 재계산 결과 중간/끝에 추가로 붙어 있었음). 이는 두 가지로 설명 가능하고 둘 다 배제할
수 없다:

- (a) 원문이 캐싱 이후 편집·확장됨(이 블로그는 "A/S" 후속 글 관행이 있고, 저장소는 원문을
  보관하지 않아(D2) 캐싱 시점의 원문과 지금의 원문이 같다는 보장이 없다) — 4건 중
  `224049461132`(원전주 폭락 이유)·`224333132194`(국민연금 리밸런싱, 위 인용 실패 4번과 동일
  글)는 재계산 결과 **본문 맨 앞에 문장이 추가**되어 있어 이 쪽에 더 가깝다.
- (b) **`one_liner_last()`의 재현 가능한 버그**: PS 노트 제거 정규식(`r"\s*PS\s*\)"`)이
  대문자 `PS)` 만 잡고 **소문자 `ps)` 를 못 잡는다**(대소문자 무시 플래그 없음). 실제
  이 블로그는 `ps)` 소문자를 흔히 쓴다. 이번 표본의 `224207570942`(중국배 호르무즈해협)
  가 정확히 이 경우 — 원문이 `"...관전포인트임. ps) 호르무즈해협 봉쇄와 관련된
  내용인데... https://youtube.com/..."` 로 끝나는데, 재계산한 `one_liner_last()` 는 이
  `ps)` 이하 유튜브 링크까지 통째로 붙여버렸다. 코드로 직접 재현 확인함(`re.split(r"\s*PS
  \s*\)", tail)` 이 소문자 `ps)` 를 통과시킴). **표본 밖에서도 전수 확인**: 캐시 736편의
  `one_liner` 필드 중 **21편**이 지금도 `ps)`(대소문자 무시)를 포함한 채 저장돼 있다 —
  즉 이 버그로 오염된 `one_liner` 가 표본 4건에 그치지 않고 전체 캐시에 21건 있다는 뜻.
  (`scripts/mer_extract.py` 는 읽기만 하라는 지시에 따라 **고치지 않았음** — 수정은
  `_ONE_LINER_MARK`/PS-분리 정규식에 `re.IGNORECASE` 를 추가하는 한 줄이면 될 것으로
  보이나 별도 승인 필요.)
- 나머지 2건(`224402375244` ISA A/S, `224207570942` 는 위 (b)로 설명됨)도 캐시보다 재계산
  결과가 훨씬 길고 구체적인 숫자(영국 ISA 한도, 일본 NISA 조건 등)가 **중간에** 추가돼
  있어 (a)에 더 가까워 보이지만, 원문을 캐싱 시점으로 되돌릴 방법이 없어 확정할 수 없다.

### 224339898546 재확인(직전 REVIEW.md 메모 후속)

이전 실측에서 `impacts[].direction: "0"` 위반이 있었다가 사라졌다고 기록된 레코드를 이번에
다시 열람. 지금은 `direction` 값이 전부 `+`/`-`/`±` 중 하나로 정상이고, `_meta` 는
`{"model":"claude-opus-5","source":"research-backfill-376"}`(`extractedAt` 없음) —
위에서 확인한 "게이트 미적용 376편" 배치에 속한다는 것과 일치.

### 여전히 사람 몫으로 남는 것

- **chain 의 개연성**과 **stance.why 의 방향 정합성** — 인용이 진짜 있었는지는 이제
  기계로 확인했지만, 그 인용들로부터 `chain`/`stance` 서술이 "합리적으로 도출됐는지"는
  의미 판단이라 자동화하지 못했다.
- **one_liner 의 대표성**(그 글의 결론을 왜곡 없이 담았는지) — 이번 표본검수는 "지금 원문에
  다시 돌렸을 때 같은 문자열이 나오는지"만 확인했다. 원문이 캐싱 이후 바뀌었을 가능성이
  있어(위 참고) 코드 실행 결과가 달라도 원래 추출이 틀렸다고 단정할 수 없다 — 이건
  캐싱 시점 원문을 보관하지 않는 결정(D2)의 근본적 한계다.
- `claude-opus-5` 376편 전체가 게이트를 거치지 않았다는 사실이 이번에 드러난 이상, **표본
  15편 밖의 나머지 361편에도 같은 "부연 생략형" 인용이 있을 수 있다** — 이번 결과는 그
  361편까지 보장하지 않는다.

## 복구 패스 결과 (2026-09-15, 검수 직후)

검수가 찾은 두 결함을 실제로 고쳤다. **검수 결과 표는 복구 전 실측이라 그대로 둔다.**

### ① 축자가 아닌 인용 제거

게이트를 거치지 않은 376편(`_meta.extractedAt` 없는 배치)의 원문을 전부 다시 받아
`quote_is_real()` 로 대조하고, 통과 못 한 인용이 붙은 항목을 **항목째** 제거했다.
인용만 지우고 항목을 남기지 않은 이유는 근거 없는 셀이 화면에 그려지면 안 되기 때문이다.

| 필드 | 제거 |
|---|---|
| indicators | 89 |
| thresholds | 39 |
| impacts | 64 |
| **합** | **192** (전체 3,816 항목의 5.0%) |

표본에서 관측된 비율(8/110 = 7.3%)과 같은 크기다. 레코드 수는 736 그대로이고 삭제된 글은 없다.
둥근따옴표(U+2018/2019) 글리프 차이로 탈락하던 오탐 1건은 따옴표만 정규화해 살렸다 —
다른 문자 정규화는 하지 않았다. 느슨하게 만들면 게이트 자체가 무의미해진다.

**독립 검증**: 복구 뒤 같은 376편을 다시 전수 대조했다(원문 재수신 376/376 성공, 실패 0).
**추가로 제거될 항목 0건** — 남은 인용은 전부 원문에 실재한다.

### ② `ps)` 오염된 one_liner 재계산

`one_liner_last()` 의 PS 분리 정규식에 `re.IGNORECASE` 를 넣은 뒤(`scripts/mer_extract.py`),
오염된 21편을 원문 재수신해 재계산했다. 현재 캐시에 `ps)` 를 품은 `one_liner` 는 **0건**이다.

### 집계에 미친 영향

근거가 빠진 만큼 보드도 줄었다 — 이게 정직한 방향이다.

| 항목 | 복구 전 | 복구 후 |
|---|---|---|
| 정규화 엣지(n≥2) | 75 | 71 |
| 그래프 노드 | 48 | 47 |
| 이벤트 | 27 | 24 |
| 재고 일수 | 11 | 7 |

지표 33 · 레벨 트리거 42 · 뷰 자산 49 · MRI 66 은 변하지 않았고, 조인 상태도 그대로다
(us10y 주시 5.0 / jgb30y 돌파 4.0 / kospi 정상 / usdjpy 정상).

### 남은 한계

- 이제 캐시 736편 **전량**이 `quote_is_real()` 을 통과한 상태다. 다만 이 게이트는
  「원문에 그 문자열이 있는가」만 본다 — **그 인용이 그 맥락을 대표하는가**는 여전히 사람 몫이다.
- `chain` 의 개연성, `stance.why` 의 방향 정합성도 자동화하지 못했다(검수 절 참고).
- 원문을 보관하지 않으므로(결정 D2) 캐싱 시점의 원문과 지금의 원문이 같다는 보장은 없다.
  글이 나중에 편집되면 지금 통과한 인용도 언젠가 대조에 실패할 수 있다.
