*.bak
.claude/
__pycache__/
*.pyc
.kis_token_cache.json
.pmi_cache.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# [이슈9] 시크릿/로컬 전용 설정 — 절대 커밋 금지 (공개 저장소)
#   .env / .env.local → 위 .env* 가 커버, .dev.vars → 위 .dev.vars* 가 커버.
#   아래 둘은 위 글롭이 못 잡아 명시 추가.
*.secret
wrangler.toml.local

# OMC 런타임 상태 — 세션별 산출물, 커밋 대상 아님
.omc/
